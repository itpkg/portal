<% @title = t '.title', id: @tag.id %>
<h3><%= @title %></h3>
<hr/>
<%= simple_form_for(@tag) do |f| %>
    <%= f.input :name, autofocus: true %>
    <%= f.button :submit, t('buttons.submit') %>
    <%= link_to t('buttons.back'), :back %>
<% end %>